# Tasks

- [ ] Ask user to save the full dataset as `ev_dataset.csv` in the `backend` folder (since chat truncated the text).
- [ ] Install `csv-parser` in the backend.
- [ ] Create a data service in backend to parse the CSV and extract averages (Efficiency, Temperature impact).
- [ ] Update `/api/optimize` logic to use the data-driven model.
- [ ] Update frontend `InputForm.jsx` to collect Temperature.
